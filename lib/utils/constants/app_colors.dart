import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.green;
  static const lightBackground = Color(0xfff2f2f2);
  static const darkBackground = Color(0xff0d0c0c);
  static const darkGrey = Color(0xff343434);
  static const grey = Color(0xffbebebe);
  static const Color primaryColor = primary;
  static const Color backgroundColor = darkBackground;
  static const Color cardColor = Color(0xFF212127); // A slightly lighter dark for contrast
  static const Color textColor = Colors.white;
  static const Color hintColor = Colors.white54;
  static const Color accentColor = Color(0xFF03DAC6);
  static const Color primaryLight = Color(0xFF4CAF50);
}